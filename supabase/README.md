# Supabase Baseline

This folder mirrors the canonical ingest foundation used by `packages/db/schema.sql`.

Keep the first migration here aligned with the host DB package so local Docker, Railway, and Supabase all share the same raw-to-canonical-to-activation model.
